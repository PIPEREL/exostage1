<form method="post" action="{{ path('annonce_delete_image', {'id': image.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ image.id) }}">
    <button class="btn btn-danger"> <i class="fa fa-trash " aria-hidden="true "></i> retirer </button>
</form>