{% extends 'users.html.twig' %} {% block title %}Page utilisateur!{% endblock %} {% block body %} {% for message in app.flashes('message') %}
<div class="alert alert-success" role="alert">{{ message }}</div>{% endfor %}
<h3 class="pl-2 mt-3">Profil de {{app.user.name}} {{app.user.firstname}}</h3>
<div class="container w-50 d-flex mt-5  align-items-center p-3 flex-column gap-3 border rounded">
    <div class="d-flex gap-2">
        <div>Nom:</div>
        <div>{{app.user.name}}</div>
    </div>
    <div class="d-flex gap-2">
        <div>Prenom:</div>
        <div>{{app.user.firstname}}</div>
    </div>
    <div class="d-flex gap-2">
        <div>Email:</div>
        <div>{{app.user.email}}</div>
    </div>
    <div class="d-flex justify-content-center gap-3">
        <div>
            <a href="{{path('users_edit')}}" class="btn btn-primary">Modifier le profil</a>
        </div>
        <div>
            <a href="{{path('users_pass_modifier')}}" class="btn btn-primary">Modifier le mot de passe</a>
        </div>
    </div>
</div>


<h3 class="pl-2 mt-3">Articles de {{app.user.name}} {{app.user.firstname}}</h3>
<div class="container w-75 d-flex mt-3  p-3 flex-column gap-3  ">
    {% for annonce in app.user.annonces %}
    <div class="d-flex justify-content-around">
        <div>titre:</div>
        <div>{{annonce.title}}</div>
    </div>
    {% endfor %} {% endblock %}